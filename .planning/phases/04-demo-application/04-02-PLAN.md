---
phase: 04-demo-application
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - projects/ngx-mkd/src/lib/ngx-mkd.ts
  - projects/demo-ngx-mkd/src/app/app.html
  - projects/demo-ngx-mkd/src/app/app.css
autonomous: true
gap_closure: true
requirements: []
---

<objective>
Fix theme toggle and code highlighting issues in the demo application.

Purpose: Close UAT gaps where theme toggle doesn't affect the rendered markdown, and copy button appears next to language label instead of top-right.
Output: Working theme toggle and properly positioned copy buttons
</objective>

<context>
@.planning/phases/04-demo-application/04-UAT.md
@projects/ngx-mkd/src/lib/ngx-mkd.ts
@projects/demo-ngx-mkd/src/app/app.html
@projects/demo-ngx-mkd/src/app/app.css

**Gap 1 - Theme Toggle:**
- Issue: ::ng-deep fails to penetrate NgxMkdComponent's ViewEncapsulation.Emulated
- Root Cause: No theme input on NgxMkdComponent, demo tries to use ::ng-deep to penetrate encapsulation
- Fix: Add `theme` input to NgxMkdComponent, use CSS custom properties internally

**Gap 2 - Code Highlighting:**
- Issue: copy-button appears next to language label, not top-right
- Root Cause: Missing z-index on .copy-button (needs z-index: 2) and .code-lang (needs z-index: 1)
- Also: Static highlight.js import with no theme switching
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add theme input to NgxMkdComponent</name>
  <files>projects/ngx-mkd/src/lib/ngx-mkd.ts</files>
  <action>
1. Add `theme` input to NgxMkdComponent:
   - Add `theme = input<'light' | 'dark'>('light');` after the markdown input

2. Update component styles to use CSS custom properties:
   - Replace hardcoded colors with CSS variables like `--code-bg`, `--code-text`, `--lang-bg`, etc.
   - Define CSS variables that respond to the theme input

3. Fix z-index issue:
   - Add `z-index: 1` to `.code-lang` selector
   - Add `z-index: 2` to `.copy-button` selector

4. Keep ViewEncapsulation.Emulated (required for Angular)

5. Ensure highlight.js static import remains (will be overridden by CSS variables for dark theme)
  </action>
  <verify>npm run build --workspace=projects/ngx-mkd 2>&1 | head -30</verify>
  <done>Component accepts theme input and applies CSS variables based on theme value</done>
</task>

<task type="auto">
  <name>Task 2: Update demo app to pass theme to NgxMkdComponent</name>
  <files>projects/demo-ngx-mkd/src/app/app.html</files>
  <action>
1. Bind theme input to NgxMkdComponent:
   - Change `<lib-ngx-mkd [markdown]="markdownSource()"></lib-ngx-mkd>`
   - To: `<lib-ngx-mkd [markdown]="markdownSource()" [theme]="isDark() ? 'dark' : 'light'"></lib-ngx-mkd>`

2. Remove the wrapper div's gh-dark class binding since theme is now passed to component:
   - Keep `[class.gh-dark]="isDark()"` for any parent styling needs, but the component handles its own theme
  </action>
  <verify>npm run build --workspace=projects/demo-ngx-mkd 2>&1 | head -30</verify>
  <done>Demo app passes theme to NgxMkdComponent</done>
</task>

<task type="auto">
  <name>Task 3: Clean up demo app CSS (remove ineffective ::ng-deep)</name>
  <files>projects/demo-ngx-mkd/src/app/app.css</files>
  <action>
1. Remove the ::ng-deep selectors that no longer work (lines 140-181):
   - The component now handles its own theming via CSS variables

2. Keep the basic dark mode styles for the header, source panel, and preview panel background

3. The .preview-content.gh-dark ::ng-deep .markdown-body section can be removed or simplified
  </action>
  <verify>npm run build --workspace=projects/demo-ngx-mkd 2>&1 | head -30</verify>
  <done>CSS cleaned up, no ineffective ::ng-deep rules</done>
</task>

</tasks>

<verification>
After build passes, test in browser:
1. Default load shows light theme in preview
2. Click theme toggle - preview should switch to dark theme
3. Code blocks show copy button in top-right corner, language label in top-left
</verification>

<success_criteria>
- Theme toggle now affects the NgxMkdComponent preview
- Copy button appears in top-right of code blocks (not next to language label)
- Build completes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-demo-application/04-02-SUMMARY.md`
</output>
