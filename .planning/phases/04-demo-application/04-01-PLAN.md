---
phase: 04-demo-application
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - projects/demo-ngx-mkd/src/app/app.ts
  - projects/demo-ngx-mkd/src/app/app.html
  - projects/demo-ngx-mkd/src/app/app.css
autonomous: true
requirements:
  - LAYO-01
  - LAYO-02
  - LAYO-03
  - LAYO-04
  - LAYO-05
  - LAYO-06
  - LAYO-07
  - THEM-01
  - THEM-02
  - THEM-03
---

<objective>
Build demo-ngx-mkd with split-pane layout, fixed header, theme toggle, and editable markdown source panel with live preview.
</objective>

<context>
@.planning/phases/04-demo-application/04-CONTEXT.md

# Key decisions from CONTEXT.md:
- Theme toggle: Wrapper class toggle (.gh-dark) on markdown-body
- Toggle button: Icon only (sun/moon)
- Layout: Tailwind Grid, 50/50 split, 56px header
- Source panel: Editable textarea with 300ms debounce
- Sample content: Full feature demo, multiple languages, includes table

Existing code:
- NgxMkdComponent at projects/ngx-mkd/src/lib/ngx-mkd.ts (fully functional)
- Demo app skeleton at projects/demo-ngx-mkd/src/app/app.ts
- Tailwind CSS v4 configured
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add layout and theme state to App component</name>
  <files>projects/demo-ngx-mkd/src/app/app.ts</files>
  <action>
Add to App component:
1. Import NgxMkdComponent from ngx-mkd
2. Add `theme` signal initialized to 'light' 
3. Add `markdownSource` signal with rich sample content (40-60 lines, multiple code languages, table)
4. Add `isDark` computed signal that returns true when theme is 'dark'
5. Add `toggleTheme()` method that cycles between light/dark and saves to localStorage
6. On init, read from localStorage to restore saved theme (default to 'light')

Sample markdown should include:
- Multiple heading levels
- Paragraphs with bold/italic
- Code blocks in JavaScript, Python, TypeScript (to show highlighting)
- A GFM table
- Lists (ordered and unordered)
- Blockquotes
</action>
  <verify>
  <automated>grep -q "theme = signal" projects/demo-ngx-mkd/src/app/app.ts && grep -q "toggleTheme" projects/demo-ngx-mkd/src/app/app.ts</automated>
  </verify>
  <done>App component has theme signal, toggleTheme method, and sample markdown content</done>
</task>

<task type="auto">
  <name>Task 2: Implement split-pane layout with header</name>
  <files>projects/demo-ngx-mkd/src/app/app.html</files>
  <action>
Create the demo app layout:

1. Header (fixed, 56px height):
   - Left: Project title "ngx-mkd" 
   - Right: Theme toggle button (sun icon ☀ for light, moon icon 🌙 for dark)

2. Two-panel Grid below header:
   - Left panel (50%): Editable textarea bound to markdownSource
     - Monospace font styling
     - Full height, independent scroll
   - Right panel (50%): NgxMkdComponent rendering markdownSource
     - Apply .gh-dark class when isDark() is true
     - Full height, independent scroll

Use Tailwind CSS classes for layout.
</action>
  <verify>
  <automated>grep -q "grid.*grid-cols-2" projects/demo-ngx-mkd/src/app/app.html && grep -q "ngx-mkd" projects/demo-ngx-mkd/src/app/app.html</automated>
  </verify>
  <done>Split-pane layout with header, source textarea, and NgxMkdComponent preview</done>
</task>

<task type="auto">
  <name>Task 3: Style the demo application</name>
  <files>projects/demo-ngx-mkd/src/app/app.css</files>
  <action>
Add component-specific styles:

1. Header: Fixed position, z-index, border-bottom, background
2. Theme toggle button: Icon styling, hover effects
3. Left panel (textarea): 
   - Monospace font
   - Full height 
   - No resize
   - Padding for comfort
4. Right panel:
   - Padding for markdown content
   - Scroll behavior
5. Add .gh-dark class styles for dark mode:
   - Override markdown-body colors for dark theme
   - github-markdown-css dark mode support via .gh-dark class
</action>
  <verify>
  <automated>test -f projects/demo-ngx-mkd/src/app/app.css && wc -l projects/demo-ngx-mkd/src/app/app.css</automated>
  </verify>
  <done>Demo app styled with header, panels, and dark mode support</done>
</task>

</tasks>

<verification>
[Build verification]
- npm run build should complete without errors
- App should serve without console errors
</verification>

<success_criteria>
- Header is fixed at top with title on left, theme toggle on right
- Two-panel layout with 50/50 split
- Left panel is editable textarea with monospace font
- Right panel shows live rendered markdown
- Theme toggle switches between light/dark
- Dark mode applies .gh-dark class to markdown-body
- Theme preference persists in localStorage
- Sample markdown includes multiple code languages and a table
</success_criteria>

<output>
After completion, create `.planning/phases/04-demo-application/04-01-SUMMARY.md`
</output>
